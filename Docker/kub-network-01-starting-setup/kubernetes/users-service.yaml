apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  selector:
    app: users
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080 # 서비스가 노출할 포트
      targetPort: 8080 # 서비스(Docker 이미지로 만든 클라이언트 or 서버) 포트

